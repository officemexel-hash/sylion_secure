---
collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.crypto
    version: ">=2.15.0"
  # Phase B (Vault adapter): uncomment when ADR-vault-adapter-001 Phase A deployed
  # - name: community.hashi_vault
  #   version: ">=6.0.0"

# Roles can be added here if needed:
# roles: []
